#include <iostream>
// #include<bits/stdc++.h>//this header include header for all container but prefer including seperate header for a particular container
#include <vector>
using namespace std;
int main()
{

    // stack<int> s;
    // 1-d vector and its grow like 1->2->4->8
    // vector<int> vec;//intialize an empty vector
    // vec.push_back(2);
    // vec.emplace_back(3);//same as push back
    // vec.push_back(4);
    // vec.pop_back();//its remove one element at a time
    // vec={1,2,3,4};//intialize elements with vector
    // or
    // vector<int> vec={1,2,3,4};
    // vector<int> vec(4,2);//this create vector of size 4 and add default value 2 in all

    // 2-d vector like matrix and its grow like 1->
    //  vector<vector<int>> vec;
    //  vec.push_back({1,2,3});
    //  vec.push_back({4,5,6});
    //  vec.push_back({7,8,9});
    //  vec.push_back({7,8,9});
    // // vec.pop_back(); //its remove element of 1 row at a time
    //  for (vector<int> element:vec)
    //  {
    //      for (int ele:element)
    //      {
    //         cout<<" "<<ele<<" ";
    //      }
    //      cout<<"\n";
    //  }

    // 3-d vector like cube and its grow by 
    // vector<vector<vector<int>>> vec;
    // vec.push_back({{1,2,3},{4,5,6},{7,8,9}});
    // vec.push_back({{1,2,3},{4,5,6},{7,8,9}});
    // vec.push_back({{1,2,3},{4,5,6},{7,8,9}});
    // vec.pop_back();//its remove element of 1 matrix at a time
    // for (vector<vector<int>> a1 : vec)
    // {
    //     for (vector<int> a2 : a1)
    //     {
    //         for (int element : a2)
    //         {
    //             cout << " " << element << " ";
    //         }
    //         cout << "\n";
    //     }
    //     cout << "\n";
    // }
    return 0;
}